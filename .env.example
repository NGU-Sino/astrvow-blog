# Keystatic 配置
# GitHub OAuth App 的 Client ID（从 https://github.com/settings/developers 获取）
KEYSTATIC_GITHUB_CLIENT_ID=

# GitHub OAuth App 的 Client Secret
KEYSTATIC_GITHUB_CLIENT_SECRET=

# 加密密钥（至少 32 位随机字符串，用 `openssl rand -hex 32` 生成）
KEYSTATIC_SECRET=